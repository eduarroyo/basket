---
codigo: BAS-3
estado: Planificado
tags:
  - tasks
---

# BAS-3: Tareas

- [x] **Bloqueante**: conseguir acceso a una suscripción de Azure desde esta máquina (`az login` funcionando, `az account show` devuelve una suscripción válida — suscripción `basic`, `ce358a47-4bfd-46cb-b4cf-d54e092d08b9`).
- [x] Instalar la Aspire CLI más reciente si hace falta y comprobar `aspire --version`/`aspire doctor` antes de empezar (skill `aspire`, "Aspire evoluciona rápido").
- [x] Ejecutar `aspire add azure-appcontainers` en el `AppHost` y configurar `builder.AddAzureContainerAppEnvironment(...)`.
- [x] Exponer `Web` públicamente (`WithExternalHttpEndpoints()`) referenciando el entorno de Container Apps.
- [x] Ejecutar `aspire add azure-sql` y sustituir `AddSqlServer("sql")` por el recurso Azure-aware (contenedor en local con `RunAsContainer()`, Azure SQL Database Serverless real al desplegar — `architecture.md` punto 3), manteniendo el interruptor `Sql:Ephemeral` para los tests (`BAS-2`).
- [x] Ejecutar `aspire add azure-keyvault` y referenciar el Key Vault desde `Web`.
- [x] Comprobar si `AddContainerRegistry`/`WithContainerRegistry` (GHCR) sigue funcionando con la versión de Aspire instalada — sigue funcionando técnicamente, pero `aspire deploy --list-steps` reveló que `AddAzureContainerAppEnvironment` aprovisiona su propio ACR de todos modos (se use o no para las imágenes); GHCR no evita ese coste, así que se revierte a aceptar el ACR por defecto del entorno (`architecture.md` punto 12, revisado).
- [x] `aspire deploy --list-steps` (o equivalente) para revisar en seco qué se va a aprovisionar antes de tocar Azure de verdad.
- [x] Primer aprovisionamiento/despliegue real con `aspire deploy` — con dos ajustes descubiertos durante el despliegue, documentados en `spec.md`: (1) Azure SQL pasa de Managed Identity a autenticación SQL (el script de asignación de roles de Aspire falla de forma determinista con Key Vault presente), con un login de aplicación de privilegios mínimos creado por un paso de *pipeline* propio (`provision-sql-app-login`, `Microsoft.Data.SqlClient` directo, no el módulo de PowerShell roto); (2) nombre de servidor SQL fijo (`basketbasetracker-sql`) para evitar referenciar el output del recurso en tiempo de ejecución. App accesible en `https://web.ashymoss-b1c8f995.spaincentral.azurecontainerapps.io`.
- [x] Configurar en Key Vault las credenciales del *seed* del primer administrador (`Seed:AdminEmail`, `Seed:AdminPassword`) de producción — `Web` las lee vía `AddAzureKeyVaultSecrets("kv")` en `Program.cs` (paquete `Aspire.Azure.Security.KeyVault`).
- [x] Verificar manualmente: la aplicación responde por HTTPS en la URL pública de Container Apps (`curl -I` → `200 OK`).
- [x] Verificar manualmente: `dotnet ef database update` aplica la migración de Identity contra la base de datos de producción como paso explícito (sin arrancar la app) — aplicado a mano con el login admin de SQL como paso puente (la automatización en CI/CD es la tarea de más abajo, aún pendiente).
- [x] Verificar manualmente: el *seed* idempotente crea el primer administrador en producción a partir de las credenciales de Key Vault, y ese administrador puede iniciar sesión — confirmado en la base de datos (`AspNetUsers`/`AspNetRoles`) y con un login real contra `/Admin/Login` (302 a `/` + cookie de autenticación).
- [ ] ~~Revisar el Bicep generado y versionarlo en `infra/`.~~ No aplica: el Bicep que produce `aspire deploy` se genera en memoria/temporal, no se escribe a disco, así que no hay nada que versionar (aclaración ya recogida en `spec.md`).
- [x] Configurar autenticación OIDC (Federated Identity) entre GitHub Actions y Azure para el job de despliegue (sin secretos de cliente en claro) — App Registration `basketbasetracker-github-actions` (`appId` `4936d6db-3ae9-45b9-8f58-90dfdfe58741`), credencial federada `github-actions-main` (subject `repo:eduarroyo/basket:ref:refs/heads/main`), roles `Contributor` en `rg-basketbasetracker-prod` y `AcrPush` en el ACR del entorno; identificadores publicados como *repository variables* en GitHub (`AZURE_CLIENT_ID`, `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID`).
- [x] Añadir el job de despliegue: `ci.yml` amplía su trigger a `[develop, main]` (sin cambios de contenido); `publish.yml` (nuevo, `push` a `main`) hace build+test, login OIDC, `az acr login` y `dotnet publish` con SDK Container Support (tag = SHA corto, sin Dockerfile) hacia el ACR, y dispara `deploy.yml` vía `workflow_dispatch` con ese `image_tag`; `deploy.yml` (nuevo, solo `workflow_dispatch`) aplica `dotnet ef database update` (login `sqladmin`), despliega la imagen como revisión nueva de Container Apps **sin tráfico** (`--revision-suffix`), corre el smoke E2E (`Tests.E2E`, modo "URL externa" vía `E2E_EXTERNAL_URL`) contra la URL de esa revisión, y solo si pasa mueve el 100% del tráfico con `az containerapp ingress traffic set` — si falla, la revisión rota queda sin tráfico, sin necesidad de rollback. Los dos puntos abiertos quedan resueltos así: (1) `deploy.yml` usa `az containerapp update --image` directo en vez de `aspire deploy`, así que `provision-sql-app-login` (que sí necesitaría el firewall) no se vuelve a ejecutar en CI — solo `dotnet ef database update` necesita conectar a Azure SQL, y se resuelve abriendo una regla de firewall temporal por ejecución (`gha-${{ github.run_id }}`), borrada al final del job (`if: always()`); (2) el riesgo de `dotnet ef database update` *forward-only* ante un rollback a un `image_tag` antiguo sigue documentado y sin mitigar, sin cambios respecto a la decisión original. Requiere además cuatro pasos manuales de *bootstrap* contra Azure/GitHub, de una sola vez, ya aplicados (detalle y motivación en `spec.md`): (1) rol `Key Vault Secrets User` para la identidad de CI sobre `kv-vrvkmus7ak2fy` (lee las credenciales del admin real para el smoke test de login); (2) secreto de GitHub `AZURE_SQL_ADMIN_PASSWORD` — la contraseña original del login `sqladmin` no quedó persistida en ningún sitio recuperable en la máquina de desarrollo (no se guardó como *user secret* de Aspire), así que se rotó con `az sql server update --admin-password` y el valor nuevo es el que se guardó como secreto; (3) `az containerapp revision set-mode --mode multiple` — la Container App `web` seguía en modo `Single` revisión (nunca cambiado desde el primer despliegue), que sustituye automáticamente la revisión activa en cada despliegue y por tanto es incompatible con el diseño de `deploy.yml` (revisión nueva sin tráfico hasta que pasa el smoke test); no es un recurso de primera clase de `AppHost.cs`, así que `aspire deploy` no lo configura; (4) tráfico de `web` fijado explícitamente por nombre de revisión (`az containerapp ingress traffic set --revision-weight web--0000002=100`, no el `latestRevision: true` automático que traía por defecto) para que las revisiones nuevas nazcan sin tráfico.
- [x] Configurar alertas básicas de Azure Monitor (latencia p95 > 200ms, tasa de error > 0,5%, fallo de health checks) con notificación por email — Application Insights no estaba implementado pese a que `spec.md` ya lo daba por decidido (comprobado: sin recurso `Microsoft.Insights/components`, exportador de Azure Monitor comentado en `ServiceDefaults`); se completa aquí: `AddAzureApplicationInsights("app-insights")` en `AppHost.cs` (solo modo publish, como Key Vault) + `web.WithReference(appInsights)` + exportador `UseAzureMonitor()` activado en `ServiceDefaults/Extensions.cs` cuando existe `APPLICATIONINSIGHTS_CONNECTION_STRING`. Alertas creadas directamente con `az` (no son recurso de primera clase de Aspire, `spec.md` ya lo documentaba): grupo de acción `ag-basketbasetracker-alerts` (email); `alert-latencia-p95` y `alert-tasa-error`, *scheduled query rules* (KQL sobre el propio Application Insights) en vez de alertas de métrica simples, porque ni `requests/duration` ni `requests/failed` soportan agregación de percentil ni de ratio en la API de métricas de Azure Monitor — solo Average/Maximum/Minimum y Count; `alert-fallo-health-check`, alerta de métrica sobre `RestartCount` (Container Apps) como *proxy* de fallo de *health check* — no hay Availability Tests en este incremento (`spec.md`, MF-3), así que es la señal más cercana disponible. Detalle completo, incluida la caída en producción encontrada al desplegar (bug de `OnConfiguring` + *pooling* de EF Core, sin relación con Application Insights) y su corrección, en `spec.md`.
- [x] Verificar manualmente: Application Insights recibe logs/trazas/métricas del entorno desplegado — confirmado con tráfico real tras el despliegue (`az monitor app-insights query`: peticiones `GET /Index` como `request` y logs de arranque como `trace`).
- [x] Verificar el flujo completo de extremo a extremo: merge a `main` → CI dispara el job de despliegue → build + push + migración + smoke E2E + despliegue → app accesible con los cambios — confirmado con dos merges reales a `main` (PR #5 y #7). El primero (PR #5) reveló dos bugs en `deploy.yml`, ninguno relacionado con Application Insights ni con el fix de `OnConfiguring`: (1) `dotnet ef database update` fallaba con `NETSDK1004` porque el proyecto `Web` nunca se restauraba antes de ese paso (`dotnet tool restore` solo restaura herramientas, no el proyecto); (2) el paso de limpieza fallaba porque `az sql server firewall-rule delete` no admite `--yes`. Producción no se vio afectada (el fallo ocurría antes de tocar la Container App); regla de firewall huérfana limpiada a mano. Corregido en PR #6, promocionado a `main` en el PR #7 — ese segundo merge completó el flujo de verdad: `publish.yml` construyó y publicó la imagen (tag `7dda8c8`), disparó `deploy.yml`, que aplicó la migración, desplegó `web--sha-7dda8c8` sin tráfico, pasó el smoke E2E y promocionó el tráfico — confirmado con `az`: revisión sana al 100% del tráfico, firewall temporal cerrado, app accesible.
- [ ] Revisar la Definición de Hecho (`workflow.md`) antes de abrir el PR a `develop`.
